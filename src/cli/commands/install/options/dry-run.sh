#!/usr/bin/env bash
# summary: Local install option: dry run (post-command parsing supported)
# group: install-options
# Sets DRY_RUN flag when sourced (legacy pre-command still works via global options)
export DOT_OPT_DRY_RUN=1
