#!/usr/bin/env bash
# summary: Option: ignore saved component selection and use all components
# group: options
# Sets flag so install_selection returns all components regardless of saved state
export DOT_IGNORE_SELECTION=1
